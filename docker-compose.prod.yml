version: '3.8'

services:
  web:
    logging:
      driver: journald
  phpmyadmin:
    logging:
      driver: journald
  db:
    logging:
      driver: journald

  crond:
    logging:
      driver: journald
    volumes:
      - /mnt/backup/db_backup:/backup
